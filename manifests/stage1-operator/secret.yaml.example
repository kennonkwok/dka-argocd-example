---
# Datadog Secret Example
#
# This file is a template showing the required structure for the Datadog API credentials.
# DO NOT commit the actual secret to Git!
#
# To use this:
# 1. Copy this file to secret.yaml in the same directory
# 2. Replace YOUR_DATADOG_API_KEY and YOUR_DATADOG_APP_KEY with your actual keys
# 3. Create the secret manually BEFORE deploying the root application:
#    kubectl apply -f manifests/stage1-operator/secret.yaml
#
# Alternative: Create the secret directly with kubectl:
#    kubectl create secret generic datadog-secret \
#      -n datadog \
#      --from-literal=api-key=YOUR_DATADOG_API_KEY \
#      --from-literal=app-key=YOUR_DATADOG_APP_KEY
#
# To get your Datadog keys:
# - API Key: https://app.datadoghq.com/organization-settings/api-keys
# - App Key: https://app.datadoghq.com/organization-settings/application-keys
apiVersion: v1
kind: Secret
metadata:
  name: datadog-secret
  namespace: datadog
type: Opaque
stringData:
  api-key: "YOUR_DATADOG_API_KEY"
  app-key: "YOUR_DATADOG_APP_KEY"
